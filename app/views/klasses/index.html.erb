<p id="notice"><%= notice %></p>

<h1>Listing Klasses</h1>

<table>
  <thead>
    <tr>
      <th>Workload</th>
      <th>Distribution process</th>
      <th>Teacher</th>
      <th>Grade</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @klasses.each do |klass| %>
      <tr>
        <td><%= klass.workload %></td>
        <td><%= klass.distribution_process %></td>
        <td><%= klass.teacher %></td>
        <td><%= klass.grade %></td>
        <td><%= link_to 'Show', klass %></td>
        <td><%= link_to 'Edit', edit_klass_path(klass) %></td>
        <td><%= link_to 'Destroy', klass, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Klass', new_klass_path %>
